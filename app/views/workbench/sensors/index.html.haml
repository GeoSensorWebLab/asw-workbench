:javascript
	$(function() {
		L.Icon.Default.imagePath = '/assets';

		window.appRouter = new Workbench.Routers.WorkbenchRouter({
			element: $("#content")
		});
		var result = Backbone.history.start({ pushState: true, root: "#{Workbench::Config.mount_path}" });
	});
