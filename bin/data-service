#!/usr/bin/env node
var program = require('gitlike-cli');

function listUsers() {
  require('./list-users.js');
}

function showUser(args) {
  var run = require('./show-user.js');
  run(args.uid);
}

program
  .command('list')
    .command('users')
      .description("List all users")
      .action(listUsers)
      .parent
    .command('sensors').parent
    .command('datastreams').parent
    .parent

  .command('show')
    .command('user <uid>')
      .description("Show a user")
      .action(showUser)
      .parent
    .parent
  .parse(process.argv);

